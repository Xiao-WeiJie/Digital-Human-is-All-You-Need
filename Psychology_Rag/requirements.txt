# PsyMind RAG 对话系统 — 依赖清单
# Python >= 3.10

# ── LangChain 核心 ──
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0

# ── 向量数据库 ──
faiss-cpu>=1.7.4          # CPU 版本（如有 GPU 可换 faiss-gpu）

# ── 其他 ──
openai>=1.0.0             # OpenAI 兼容接口（DashScope 兼容层）
tiktoken>=0.5.0           # Token 计数
